# オフライン閲覧（プレゼン用）

インターネットの無い環境でも、このサイトを丸ごとブラウザで閲覧できる
**静的サイト（`out/` フォルダ）** を書き出せます。事業プレゼンなどで
ネット接続が不安なときに使ってください。

## 1. 書き出す（ネット接続のあるPCで一度だけ）

```bash
npm install        # 初回のみ
npm run build:offline
```

`out/` フォルダに、お客様向けページ一式（トップ／店舗／スポット／
イベント／おでかけ特集／レンタカー／特典 など）が静的HTMLとして
書き出されます。

> 画像について：外部の写真サービスを使わず、**アップ済みの実写真**
> （南京町・姫路城・甲子園など）と、**端末内で生成するブランド風の
> 画像**（ジャンル絵文字＋グラデーション）に自動で切り替わります。
> そのため `out/` だけ持ち運べば、ネットが無くても画像が崩れません。

## 2. オフラインで見せる（当日のPC・ネット不要）

`out/` フォルダを発表用PCにコピーし、簡易サーバーで開きます。

```bash
npx serve out          # もしくは  npm run serve:offline
```

表示された `http://localhost:3000` をブラウザで開いてください。
（`npx serve` を使う場合、その取得だけは事前にネットのある環境で
一度実行しておくと確実です）

Python が入っているPCなら、ネット無しで次でもOKです：

```bash
cd out && python3 -m http.server 8000
# → http://localhost:8000 を開く
```

## 補足

- 管理画面（スタッフ向け `/staff`・`/api`）はサーバー機能のため、
  オフライン静的版には含まれません。お客様に見せる画面のみが対象です。
- 通常のオンライン版（`npm run build` / `npm start`）は今まで通り
  動作し、影響はありません。
- 実際のイベント日程・料金はデモ用の代表値です。本番では公式情報に
  差し替えてください。
